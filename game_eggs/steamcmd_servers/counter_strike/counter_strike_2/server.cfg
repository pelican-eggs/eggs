echo ----------------------------
echo - Start loading server.cfg -
echo ----------------------------

hostname "A Pterodactyl hosted CS2 Server"	// Server name.
sv_lan 0									// Show the server online.
sv_password ""								// Protect your server with a password.
rcon_password "S3cureRCONPasswordH3re		// Allow admins to run RCON console commands or via external tools.
sv_logfile 1								// Logs server information in the log file.
sv_parallel_sendsnapshot 1					// Enables multi-threading.
sv_hibernate_when_empty 0					// Puts the server into extremely low CPU usage mode when no clients connected.

echo ---------------------------
echo - Done loading server.cfg -
echo ---------------------------

exec sourcetv.cfg